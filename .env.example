# Copy to `.env` and fill in the keys you intend to use.
# Each pipeline only needs the providers it actually calls.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
CEREBRAS_API_KEY=
TOGETHER_API_KEY=

# Only required when loading models from W&B-hosted artifacts
WB_API_KEY=
